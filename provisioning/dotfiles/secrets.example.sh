# ~/.config/shell/secrets.sh  (TEMPLATE -- copy this to secrets.sh and fill in)
#
# The real secrets.sh is NEVER committed and NEVER copied off a host by deploy.sh.
# Sourced last by both .zshrc and .bashrc, so these override anything above.

